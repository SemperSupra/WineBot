# Status - 2026-03-12

## Progress Today
- Performed full local teardown and no-cache rebuild of core images:
  - `winebot`
  - `winebot-interactive`
  - `test-runner`
  - `lint-runner`
- Ran local validation stack:
  - `./scripts/wb lint` (pass)
  - `./scripts/wb test` (pass: unit + E2E)
  - `scripts/bin/smoke-test.sh --full --include-interactive --include-debug --cleanup --no-build` (failed/hung in debug phase)
  - `scripts/bin/smoke-test.sh --full --include-interactive --cleanup --no-build` (failed/hung in embedded E2E phase)
  - In-container diagnostics:
    - `/scripts/diagnostics/diagnose-input-suite.sh` (pass)
    - `/scripts/diagnostics/diagnose-input-trace.sh` (pass with warnings/observations)
    - `/scripts/diagnostics/diagnose-trace-soak.sh` with `DURATION_SECONDS=120` (pass)
    - `/scripts/diagnostics/health-check.sh` (pass, deprecated notice)
    - `/scripts/diagnostics/diagnose-master.sh all` (failed: Notepad window not observed in smoke phase)

## Validation Status
- **Rebuild:** Passed.
- **Lint:** Passed (Trivy warning: binary not present, scan skipped).
- **Unit/Integration tests:** Passed (`135/135`).
- **E2E tests:** Passed in direct `./scripts/wb test` run (`14/14`).
- **Smoke+Debug path:** Fails due to hang (see failures).
- **Master diagnostics (all phases):** Fails in phase 3 smoke window observation.

## Confirmed Failures and Evidence
1. `smoke-test.sh` debug hang
- Trigger: `--include-debug`
- Evidence: `scripts/bin/smoke-test.sh` line invoking:
  - `docker compose ... --profile headless run --rm winebot`
- Effect: long-lived entrypoint/supervisor runs instead of bounded debug command; smoke run does not complete.

2. Intermittent window discovery failure through API
- `diagnose-master.sh all` fails with:
  - `Smoke tests: FAILED (Notepad window not observed)`
- During a stalled smoke-embedded E2E run, API returned empty windows:
  - `GET /windows` -> `{"windows":[]}`
- At the same time, in-container X11 enumeration showed active windows, including Notepad:
  - `DISPLAY=:99 /automation/bin/x11.sh list-windows`
  - `DISPLAY=:99 wmctrl -l`
- Repeated probe showed `/windows` instability right after successful Notepad launch requests:
  - Iterations returned `count=0 notepad=0` repeatedly.

3. Trace bisect warnings (non-fatal but relevant)
- `diagnose-input-trace.sh` observations:
  - X11 core trace gap (`x11_core=0`) while XI2/windows/hook layers saw events.
  - Variant-specific trace misses (`window_click`, `down_up`) in XI2 traces.
- End-to-end interaction still passed in input suite for Notepad/Regedit/Winefile.

## Runtime State at Handoff
- Running container:
  - `compose-winebot-interactive-1` (healthy)
- Stopped during troubleshooting:
  - stalled smoke/test-runner one-off containers
  - headless smoke service container after interrupted smoke runs

## Key Logs/Artifacts to Inspect Next
- Session logs (active interactive):
  - `/artifacts/sessions/<session-id>/logs/api.log`
  - `/artifacts/sessions/<session-id>/logs/input_trace.log`
  - `/artifacts/sessions/<session-id>/logs/input_events.jsonl`
  - `/artifacts/sessions/<session-id>/logs/input_events_windows.jsonl`
  - `/artifacts/sessions/<session-id>/logs/diagnostics/input_trace_bisect.log`
  - `/artifacts/diagnostics_suite/summary.json`
  - `/artifacts/diagnostics_master/master.log`

## Root-Cause Direction (Working Hypothesis)
1. Smoke debug phase bug:
- `smoke-test.sh` uses a `compose run` form that starts the normal long-running entrypoint for debug instead of a bounded command path.

2. Window discovery race/flakiness:
- API `/windows` sometimes reports empty set while X11 has visible windows.
- Likely in API-side enumeration/timing/filtering path, not raw X11/WM process availability.

## Next Steps (Ordered Resume Plan)
1. Fix smoke debug execution path
- Change debug phase to execute a bounded command (`compose exec` to running container or `run --entrypoint` with explicit non-daemon command).
- Add timeout and explicit failure condition.

2. Stabilize `/windows` API correctness
- Instrument API window enumeration path with timing, command stderr/stdout, and retry telemetry.
- Compare API enumeration command and options with `automation/bin/x11.sh list-windows`.
- Add bounded retry in API endpoint before returning empty list when X11 is healthy.

3. Add regression tests for window discovery reliability
- New deterministic test: launch Notepad via API and require `/windows` to observe Notepad within bounded retries.
- Include stress variant (multiple rapid launches/closures) and assert non-empty baseline desktop windows.

4. Re-run full validation after fixes
- `./scripts/wb lint`
- `./scripts/wb test`
- `scripts/bin/smoke-test.sh --full --include-interactive --include-debug --cleanup --no-build`
- `docker exec ... /scripts/diagnostics/diagnose-master.sh all`

5. Only after local full pass:
- prepare commit/push and proceed with any PR/release activity.

## Notes for Next Agent Session
- Do not treat these as flaky tests to suppress; current objective is root-cause correction.
- Preserve current strict policy: no masking failures, no PR before local E2E/full validation passes.
- If reproducing interactively, start from a clean compose state to avoid stale one-off container side effects.
